import React from "react";

export default function () {
    return (
        <section className={"w-full h-full flex flex-col  text-center justify-center bg-red-300"}>
            <header>
                <h1 className={"text-4xl"}>Some Beautiful Things in The France</h1>
            </header>
            <main>
                Nothing
            </main>
        </section>
    );
}